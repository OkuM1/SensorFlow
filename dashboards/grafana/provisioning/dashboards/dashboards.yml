# Grafana dashboard provisioning

apiVersion: 1

providers:
  - name: 'SensorFlow Dashboards'
    orgId: 1
    folder: 'SensorFlow'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
